services:
  pg:
    build:
      context: .
      dockerfile: pg.dockerfile
    container_name: cervo_pg
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=cervo