{
  "name": "cervo-api",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch index.ts",
    "build": "tsc",
    "pretest": "bun run build",
    "test": "bun run test",
    "lint": "biome lint src",
    "format": "biome format --write src"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.2",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "zod": "^4.0.5"
  }
}